# dependencies (only api/ uses npm now)
node_modules/

# env / secrets (gitignored, kept locally)
.env
.env.local
.deploy-secrets.txt
.vercel-env-import.txt

# Vercel artifacts from prior Next.js setup (no longer in use; safe to delete)
.vercel/
.next/

# Azure deploy artifacts from prior setup
.app-deploy-output.json
.deploy-output.json
release.zip

# Azure Functions local settings (template provided; never commit real one)
api/local.settings.json

# user's source brand artwork (kept on disk, not in git — duplicates of /bcc-logo.png)
LOGO*.png
BCC_Logo_Full*.png

# logs
*.log

# editor / OS
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/extensions.json
.idea/
infra/main.parameters.bcc.json
